# 本地性能烟雾基线

基线环境为 Windows、Python 3.11+、CPU 模式。这里记录的是防止明显性能退化的烟雾门槛，不是产品 SLA。

| 操作 | 固定输入 | 回归门槛 |
|---|---|---:|
| 加载验收清单 | 20 个 JSON 案例 | < 1 秒 |
| 构建父子 chunk | 200 个短文本块 | < 5 秒 |

真实 PDF、OCR、本地向量模型、重排和 GLM 的速度受硬件、模型缓存与网络影响，不纳入稳定 CI 门槛；应在目标机器另行记录。
